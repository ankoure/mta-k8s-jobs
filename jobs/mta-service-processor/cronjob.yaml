apiVersion: batch/v1
kind: CronJob
metadata:
  name: mta-service-processor
  namespace: data-jobs
spec:
  schedule: "0 6 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: mta-service-processor
              image: ghcr.io/ankoure/mta-service-processor:latest
              resources:
                requests:
                  memory: "512Mi"
                  cpu: "250m"
                limits:
                  memory: "4Gi"
                  cpu: "1"
              envFrom:
                - secretRef:
                    name: aws-credentials
